<Window x:Class="CalculatorApp.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Calculator" Height="250" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Calculator App" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="1" Text="Developed by Andreea Vilcu" FontSize="14" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="2" Text="Group: 10LF333" FontSize="14" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="3" Text="This application was developed as a homework project for the MVVM pattern implementation course." 
                   TextWrapping="Wrap" FontSize="12"/>

        <Button Grid.Row="4" Content="OK" Width="80" HorizontalAlignment="Right" Click="OkButton_Click"/>
    </Grid>
</Window>